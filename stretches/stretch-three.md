## Stretch Three
### Counters

```javascript
const createCounter = () => {
  // YOUR CODE
};

const firstCounter = createCounter();
const secondCounter = createCounter();

firstCounter(); // => 1
firstCounter(); // => 2
firstCounter(); // => 3

secondCounter(); // => 1
secondCounter(); // => 2

// Importantly...
firstCounter(); // => 4
```
